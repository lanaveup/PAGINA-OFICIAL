const form=document.querySelector('#contactForm');const statusEl=document.querySelector('#status');
form.addEventListener('submit',async(e)=>{e.preventDefault();const button=form.querySelector('button');button.disabled=true;button.textContent='Enviando…';statusEl.textContent='';try{const data=Object.fromEntries(new FormData(form).entries());const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json();if(!response.ok)throw new Error(result.error||'No se pudo enviar');form.reset();statusEl.textContent='¡Listo! Recibimos tu propuesta.';}catch(error){statusEl.style.color='#a42549';statusEl.textContent=error.message||'Ocurrió un error. Escríbenos a lanave@up.edu.pe.';}finally{button.disabled=false;button.textContent='Enviar propuesta ↗';}});
